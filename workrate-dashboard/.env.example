# WorkRate Dashboard — Environment Variables
# Copy to .env.local for development, set in Netlify UI for production.

# Your Railway API URL (no trailing slash).
# Leave empty in dev — Vite proxy forwards /api → localhost:3001 automatically.
VITE_API_URL=https://your-api.railway.app
